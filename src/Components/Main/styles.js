import styled from 'styled-components/native';

export const ImageDesignBottom = styled.Image`
    align-self:flex-end;
    z-index: -1;
    position : absolute;
    bottom : 0;
    margin-bottom : -60px;
`
